import * as bootstrap from "bootstrap"; // Importera Bootstrap
require("bootstrap-icons/font/bootstrap-icons.css") // Importera Bootstrap-icons för ikoner
index = require("./indexFunctions.js") // Funktioner jag har skrivit för indexfilen
calendar = require("./calendar.js") // Skript för kalendern på sidan